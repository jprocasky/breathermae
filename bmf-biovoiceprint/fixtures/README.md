# BioVoicePrint fixtures

- `sample_plain_language_report.json` – stage-7 user-facing report (for `[bmf_biovoice_report fixture="1"]`)
- `sample_bsi_pattern_payload.json` – stage-7 numerical / pattern payload (for future scores shortcode)

These are offline samples until the Python analysis engine posts real results into `wp_bm_biovoice_results`.
